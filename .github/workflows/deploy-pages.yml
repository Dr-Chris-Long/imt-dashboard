# RETIRED (run #1 failed: configure-pages enablement rejected with workflow token).
# Deployment mechanism is now the gh-pages branch, auto-enabled by user-push branch
# creation and refreshed by the mirror step in update.yml. Kept as a tombstone so the
# failure and the pivot are visible in-repo.
name: deploy-pages-retired
on:
  workflow_dispatch: {}
jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: 'true'
